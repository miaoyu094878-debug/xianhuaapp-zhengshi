# ===== 非项目文件，绝不提交/推送到 GitHub =====
# 规则：所有不属于项目本身的文件/代码，都不得进入 git。

# Agent 内部记忆文件（仅供本机跨会话读取，不属于项目代码）
AGENT_NOTES.md

# 上传/截图临时占位目录（用户上传的图片等）
.uploads/

# 网页分享/打包临时目录
.trae-html-share-packages/

# 临时脚本
_wpcrop.cjs

# 本地积分账本（本地开发模式的运行时数据，含用户余额，不入库）
data/